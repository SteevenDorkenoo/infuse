{% extends 'base.html.twig' %}

{% block title %}Éditeur de commentaire{% endblock %}

{% block body %}
   <div class="container mt-5">
    <h1>Modifier votre commentaire</h1>
    <form method="post" class="mt-4">

        <div class="form-group mt-3">
            <label for="contenu">Contenu</label>
            <input value="{{commentaire.content}}" type="contenu" id="contenu" name="contenu" class="form-control" required>
        </div>

        <input type="hidden" name="_token" value="{{ csrf_token('edit') }}">

        <button type="submit" class="btn btn-secondary btn-sm mt-3" onclick="return confirm('Êtes-vous sûr de vouloir modifier ce commentaire ?');">Enregistrer</button>
        <a href="{{ path('recipe_show',{ 'id': recipe.id}) }}" class="btn btn-secondary btn-sm mt-3">Retour</a>
    </form>
</div>
{% endblock %}