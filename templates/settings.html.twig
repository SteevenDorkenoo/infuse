{% extends "base.html.twig" %}

{% block title %}Hello RecipesController!{% endblock %}

{% block body %}

<main class="shadow rounded-4 container-lg">
    <h3>Paramètres utilisateur</h3>

    <div>
        <a href="{{ path('my_recipes') }}" class="link-underline link-underline-opacity-0">Voir mes recettes</a>
    </div>
    
    <div>
        <a href="{{ path('favorites_index') }}" class="link-underline link-underline-opacity-0">Voir mes favoris</a>
    </div>
    
    <div>
        <a href="{{ path('user_edit_cla', {'id': app.user.id}) }}" class="link-underline link-underline-opacity-0">Modifier mes informations utilisateur</a>
    </div>
    <div>
        <a href="{{path('templates/mentions.html.twig')}}">Mentions Légales</a>
    </div>
    <div>
        <a href="{{ path('app_logout')}}" onclick="return confirm('Êtes-vous sûr de vouloir vous déconnecter ?');" class="link-underline link-underline-opacity-0">Se déconnecter</a>
    </div>
</main>

{% endblock %}